# 章別ガイド

このディレクトリには、NuxtサーバーAPIの不具合を、バグ状態の観測から最小修正、回帰確認まで追う章別ガイドを置きます。

| 章 | 題材 | 状態 |
| --- | --- | --- |
| [第01章](01-create-error-response.md) | エラーJSONを`return`してHTTP 200になる問題 | 実装済み |

各章では、まずバグ再現コミットで意図したアサーション失敗を確認します。次に`main`へ戻り、同じテストが成功し、Nuxt本番ビルドも通ることを確認します。
